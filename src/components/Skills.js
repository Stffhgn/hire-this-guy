import React from 'react';

export default function Skills() {
    return (
        <div>
            <h2>Frontend Expertise</h2>
            <p>React, JavaScript (ES6+), CSS, HTML, Webpack, Redux, TypeScript</p>
        </div>
    );
}
